# Copilot Workspace Instructions

- End every response with: request time taken (wall-clock), premium requests used (or estimated/unknown), best-guess human time equivalent, and model used.
- Append one CSV row per prompt to `logs/copilot_prompt_metrics.csv` with columns: datetime, summary_lt_10_words, wall_clock_time, premium_usage, estimated_human_time, model.
