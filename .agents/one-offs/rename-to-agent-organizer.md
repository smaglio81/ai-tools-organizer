# Rename to Agent Organizer

* Please find all locations in the source code that refers to this extension as `Agent Skills` or `agentSkills` or similar derivates and replace it with `Agent Organizer` or `agentOrganizer` or equivalent.